apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: analysis-db
  name: analysis-db
  namespace: egs-fuellink
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi